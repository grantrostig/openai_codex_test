<Window
    x:Class="WinUiWhisperTranscriber.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Whisper Batch Transcriber">
    <Grid Padding="16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="8">
            <Button x:Name="select_source_audio_files_button" Content="Add audio files" Click="OnSelectSourceAudioFilesClicked"/>
            <Button x:Name="select_destination_folder_button" Content="Select destination" Click="OnSelectDestinationFolderClicked"/>
            <Button x:Name="convert_all_files_button" Content="Convert" Click="OnConvertAllFilesClicked"/>
        </StackPanel>

        <TextBlock Grid.Row="1" x:Name="selected_destination_folder_text_block" Text="Destination folder: (not selected)"/>

        <StackPanel Grid.Row="2" Spacing="8">
            <TextBlock Text="Queued files" FontWeight="SemiBold"/>
            <ListView x:Name="queued_source_audio_files_list_view" Height="170"/>
            <TextBlock Text="Conversion report" FontWeight="SemiBold"/>
            <ListView x:Name="converted_files_report_list_view" Height="170"/>
        </StackPanel>

        <TextBlock Grid.Row="3" x:Name="status_text_block" Text="Ready."/>
    </Grid>
</Window>
